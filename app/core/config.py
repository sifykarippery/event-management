# from pydantic_settings import BaseSettings
# from typing import List
# from pydantic import AnyHttpUrl

# class Settings(BaseSettings):
#     cors_origins: List[AnyHttpUrl]  # Expecting a list of valid URLs

#     class Config:
#         env_file = ".env"

# settings = Settings()
